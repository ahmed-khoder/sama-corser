# بروتوكول العمل والتعامل المعتمد (Interaction & Workflow Protocol)
### مشروع: SAMA Logistics (`sama-corser`)

تم اعتماد هذا النهج والبروتوكول بناءً على توجيه المستخدم ليكون الأساس الإلزامي في كل مهام التطوير والتعامل:

---

## 1. المبادئ الأساسية للتعامل (Core Operating Rules)
1. **التقارير الإلزامية في ملفات مستقلة:**
   - يمنع منعاً باتاً إغراق الشات بتقارير مطولة أو نصوص ضخمة.
   - يتم إنشاء وحفظ كل تقرير داخل ملف توثيق بصيغة Markdown (`.md`) في جذر المشروع أو المجلد المخصص.
   - يكون رد الشات ملخصاً وتنفيذياً ومباشراً ويشير إلى رابط الملف المُنْشأ.

2. **تقرير ما قبل التنفيذ (ما سيتم تنفيذه — Pre-Execution Plan):**
   - قبل تعديل أي ملف كود في المهام الكبيرة أو المتوسطة، يجب إعداد تقرير تفصيلي بما سيتم تنفيذه (الملفات المستهدفة، التعديلات المقترحة، قيود السلامة، وخطة الفحص).
   - التوقف التام وانتظار موافقة المستخدم الصريحة قبل الشروع في التنفيذ.

3. **تقرير ما بعد التنفيذ (ما تم تنفيذه — Post-Execution Report):**
   - فور الانتهاء من تنفيذ أي طلب، يتم إعداد تقرير كامل بالنتائج داخل ملف Markdown.
   - يتضمن التقرير:
     - قائمة بالملفات المعدلة وموجز التغيير.
     - نتيجة فحص الـ TypeScript (`npx tsc --noEmit`).
     - نتيجة بناء المشروع (`npm run build`).
     - ملخص الفروقات الفنية (Git Diff Summary).

4. **قيود السلامة الصارمة والمستمرة (Safety Invariants):**
   - **قاعدة البيانات و Prisma:** لا يتم لمس ملفات `prisma/schema.prisma` أو تعديل بيانات الـ Database إلا بطلب صريح وخطة نسخ احتياطي مسبقة.
   - **الواجهة والتصميم (UI/UX):** الحفاظ الكامل على التصميم، الألوان، الخطوط، حركات Framer Motion، ومكتبات Three.js دون أي تشويه أو تغيير غير مطلوب.
   - **الحزم والتبعيات:** لا يتم تثبيت أو حذف أي Package دون إذن صريح.
   - **عمليات Git:** لا يتم عمل `git commit` أو `git push` أو `git reset` إلا بطلب المستخدم.
