# تقرير خطة التنفيذ: تحسينات الـ Content SEO للصفحات العامة
### مشروع: SAMA Logistics (`sama-corser`)
**المرجع:** مخرجات التدقيق الشامل للـ Content SEO (مع استبعاد المدونة والمقالات تماماً).  
**الحالة:** بانتظار موافقة المستخدم الصريحة للشروع في التنفيذ (Pre-Execution Plan).  
**تاريخ التقرير:** سبتمبر 2026

---

## 1. نطاق العمل المحدد (Scope & Boundaries)

* **الصفحات المشمولة في خطة التنفيذ:**
  1. الصفحة الرئيسية (`app/page.tsx` ومكوناتها في `components/home/`).
  2. صفحة من نحن (`app/about/page.tsx`).
  3. صفحة اتصل بنا (`app/contact/page.tsx`).
  4. صفحة التوظيف العامة (`app/careers/page.tsx`).
  5. صفحة تفاصيل الوظيفة (`app/careers/[id]/JobDetailsClient.tsx`).
  6. المظلة الرئيسية للخدمات (`app/services/page.tsx` و `ServicesPageClient.tsx`).
  7. صفحات الهبوط للخدمات المتخصصة:
     - نقل الحاويات (`app/services/container-transport/`).
     - نقل المواد الصناعية (`app/services/industrial-transport/`).
     - نقل الرخام ومواد البناء (`app/services/marble-transport/`).
  8. المكونات المشتركة: الفوتر (`components/Footer.tsx`) ومكون عرض الشرائح (`components/HeroSlideshow.tsx`).

* **المستثنى تماماً وبشكل قاطع (Strictly Excluded):**
  - مجلد المدونة والمقالات بالكامل: `app/blog/**` و `components/blog/**`.
  - منطق لوحة التحكم (Dashboard) ونظام المصادقة وحسابات المستخدمين.
  - قاعدة البيانات وجداول Prisma (`prisma/schema.prisma` و `prisma/dev.db`).
  - التنسيقات البصرية والـ UI/UX و Tailwind CSS ومكتبات الحركة (Framer Motion / Three.js).

---

## 2. تفاصيل ما سيتم تنفيذه موزعاً على مراحل (Execution Phases)

### المرحلة الأولى (Phase 1): تصحيح الروابط الداخلية والفوتر (Internal Linking & Footer)
#### الملف المستهدف:
* `components/Footer.tsx`

#### التشخيص الفني:
* يحتوي كود الفوتر في `FALLBACK_SERVICES` على تسميات خاطئة لا تطابق المسارات (Slugs):
  - `container-transport` مسمى بالخطأ "الشحن البحري / Sea Freight".
  - `industrial-transport` مسمى بالخطأ "النقل البري / Land Transport".
  - `marble-transport` مسمى بالخطأ "الشحن الجوي / Air Freight".
* عند تحميل خدمات الـ CMS من قاعدة البيانات، تختفي صفحات الهبوط الثابتة المتخصصة (`container-transport`, `industrial-transport`, `marble-transport`) من الفوتر لأنها صفحات تسويقية مخصصة غير مسجلة كـ Slugs في جدول `Service`.

#### ما سيتم تنفيذه:
1. تصحيح مصفوفة `FALLBACK_SERVICES` لتطابق هويتها الحقيقية:
   ```typescript
   const FALLBACK_SERVICES = [
     { id: 'fb-1', slug: 'container-transport', titleAr: 'نقل الحاويات واللوجستيات', titleEn: 'Container Transport' },
     { id: 'fb-2', slug: 'industrial-transport', titleAr: 'نقل المعدات والبضائع الصناعية', titleEn: 'Industrial Transport' },
     { id: 'fb-3', slug: 'marble-transport', titleAr: 'نقل الرخام ومواد البناء', titleEn: 'Marble & Mining Transport' },
     { id: 'fb-4', slug: 'customs-clearance', titleAr: 'التخليص الجمركي', titleEn: 'Customs Clearance' },
   ];
   ```
2. ضمان وصول محركات البحث وزوار الموقع لصفحات الهبوط الثلاث بشكل دائم جنباً إلى جنب مع خدمات الـ CMS، لمنع أي فقدان في قوة الربط الداخلي (Link Juice).

---

### المرحلة الثانية (Phase 2): معالجة تضارب الكلمات المفتاحية (Keyword Cannibalization)
#### الملف المستهدف:
* `app/services/page.tsx`

#### التشخيص الفني:
* تتنافس صفحة الخدمات المجمعة `/services` حالياً في وسوم الـ Metadata (العنوان والوصف والكلمات الدلالية) مباشرة مع صفحة الهبوط المتخصصة `/services/container-transport` على عبارات دقيقة مثل:
  - "نقل حاويات بورسعيد"
  - "نقل الحاويات في مصر"
* هذا التنافس يجعل خوارزميات جوجل في حيرة بين ترتيب الصفحة المجمعة أو صفحة الهبوط المتخصصة.

#### ما سيتم تنفيذه:
1. إعادة ضبط صياغة `generateMetadata()` في `app/services/page.tsx` لتركز على المظلة الشاملة للخدمات اللوجستية:
   - **العنوان المقترح (عربي):** `"خدمات الشحن والحلول اللوجستية المتكاملة | سما لوجستيك مصر"`
   - **العنوان المقترح (إنجليزي):** `"Comprehensive Logistics & Freight Solutions Egypt | SAMA Logistics"`
   - **الوصف:** التركيز على تكامل الخدمات (شحن بحري، نقل بري، تخليص جمركي، تخزين وإدارة سلاسل الإمداد).
2. إبقاء صفحة `/services/container-transport` وحدها كصفحة الهبوط المستهدفة والمتصدرة لعبارات نقل الحاويات، الشحن الكامل FCL، الشحن الجزئي LCL، ومقطورات الموانئ.

---

### المرحلة الثالثة (Phase 3): تصحيح التدرج الهيكلي للعناوين (Heading Hierarchy)
#### الملفات المستهدفة:
* `app/contact/page.tsx`
* `app/careers/[id]/JobDetailsClient.tsx`

#### التشخيص الفني:
* في صفحة الاتصال `app/contact/page.tsx`:
  - يوجد عنوان رئيسي واحد `h1` (تواصل معنا / Get in Touch).
  - يتبعه مباشرة داخل الكارت الجانبي عنوان `h3` (بيانات التواصل / Contact Information) على السطر 58، متخطياً مستوى `h2` تماماً.
* في صفحة تفاصيل الوظيفة `app/careers/[id]/JobDetailsClient.tsx`:
  - يوجد `h1` لعنوان الوظيفة.
  - يليه مباشرة عناوين `h3` لأقسام ملخص الوظيفة ونموذج التقديم (الأسطر 169 و 196) دون وجود أي وسم `h2`.

#### ما سيتم تنفيذه:
1. ترقية وسوم `<h3>` المستهدفة دلالياً لتصبح `<h2>` مع **الاحتفاظ الصارم بنفس أسماء الـ Classes والتصميم والألوان بنسبة 100%**.
2. لن يطرأ أي تغيير على مظهر الصفحة أمام المستخدمين، بينما ستصبح بنية الـ HTML متوافقة كلياً مع معايير W3C و Google Webmaster Guidelines.

---

### المرحلة الرابعة (Phase 4): تحسين نصوص الصور البديلة (Image Alt-Text Optimization)
#### الملف المستهدف:
* `components/HeroSlideshow.tsx`

#### التشخيص الفني:
* في مكون عرض الشرائح الأساسي بالصفحات، يحتوي وسم الصورة على نص بديل ثابت:
  `alt="SAMA Logistics"`
* هذا النص البديل يتكرر عبر جميع الشرائح في كل الصفحات التي تستخدم المكون، مما يفوت فرصة تصدر الموقع في بحث الصور لجوجل (Google Images) المتعلق باللوجستيات والموانئ والشحن.

#### ما سيتم تنفيذه:
1. تحسين وسم `alt` في `components/HeroSlideshow.tsx` بحيث يدعم:
   - نصاً بديلاً دلالياً يعتمد على اللغة الحالية (عربي/إنجليزي).
   - سياقاً يوضح النشاط اللوجستي وموقع ميناء بورسعيد (مثلاً: *"خدمات شحن الحاويات والخدمات اللوجستية بميناء بورسعيد - سما لوجستيك"*).
2. عدم تغيير أي منطق حركي أو أداء تحميل أو أحجام الصور.

---

## 3. خطة الفحص والتحقق الصارمة (Verification & Quality Assurance Plan)

عند صدور الإذن بالتنفيذ، سيتم تطبيق الاختبارات التالية فورياً:

1. **فحص سلامة الأنواع والـ TypeScript:**
   ```bash
   npx tsc --noEmit
   ```
   *المعيار المطلوب:* الخروج بكود `0` وخلو المشروع من أي خطأ تيبينغ.

2. **بناء المشروع الإنتاجي بالكامل (Production Build):**
   ```bash
   npm run build
   ```
   *المعيار المطلوب:* نجاح بناء كافة الصفحات الثابتة والديناميكية (98+ مسار) دون أي تحذيرات حرجة أو انهيار في التوليد.

3. **فحص الروابط الحية والـ Dev Server:**
   - التأكد من استجابة المسارات المعدلة وعودتها بكود `200 OK`.
   - التأكد من صحة وسوم الـ Metadata وروابط الفوتر.

4. **إعداد تقرير ما بعد التنفيذ (Post-Execution Report):**
   - حفظ التقرير النهائي ونتائج الفحص والـ Git Diff داخل ملف markdown مستقل وإخطار المستخدم به.

---

## 4. قيود الأمان المعتمدة (Safety Invariants)
- [x] عدم تعديل أو كتابة أي بايت في مجلد `app/blog/**`.
- [x] عدم تعديل ملفات قاعدة البيانات `prisma/` أو تشغيل أي أوامر تهجير (Migrations).
- [x] عدم تعديل ملفات التنسيق `globals.css` أو إعدادات `tailwind.config.js`.
- [x] عدم تشغيل أي أوامر `git commit` أو `git push` أو تعديل إعدادات الـ Git.
- [x] التوقف التام الآن وانتظار تعليمات المستخدم.
